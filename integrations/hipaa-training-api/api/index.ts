// Built by `npm run build` before Vercel bundles this handler.
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-expect-error — dist is JS output from tsc
import app from "../dist/index.js";

export default app;
