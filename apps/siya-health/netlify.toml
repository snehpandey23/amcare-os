# DEPRECATED — Production deploys use Vercel (vercel.json).
# This file is retained for historical reference only. Do not maintain redirect parity here.
# See apps/siya-health/vercel.json and `npm run deploy`.

[build]
  publish = "apps/siya-health"
  command = "echo 'Netlify deploy disabled — use Vercel' && exit 1"
